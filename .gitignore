# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/

# Node
node_modules/
.next/
out/
*.log
*.tsbuildinfo

# Env & secrets
.env
*.env.local

# IDE / OS
.vs/
.idea/
Thumbs.db

# Playwright artifacts
.playwright-mcp/

# Ảnh crawl dung lượng lớn (giữ ngoài git)
DataBDS/data/

# Artefact sinh ra khi chạy pipeline (dataset SFT, dump) — không đưa vào git
backend/artifacts/

# Trọng số LoRA adapter bàn giao từ máy train (vài chục MB/cái) — giữ ngoài git,
# chỉ commit README mô tả hợp đồng. Xem training/README.md.
backend/models/adapters/*
!backend/models/adapters/README.txt
