# Phụ thuộc nặng cho embedding và sinh nội dung — chỉ cài trên máy chạy model,
# KHÔNG cài trong CI (test dùng backend `hashing` + provider `template`).
#
# GPU (khuyến nghị): cài torch bản CUDA trước
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
sentence-transformers==3.3.1
transformers==4.48.0
accelerate==1.2.1
bitsandbytes==0.45.0
sentencepiece==0.2.0
